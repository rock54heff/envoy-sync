package store

// GitLabDiff compares a local env map against a GitLab CI/CD variable store
// and returns the diff result using the shared Compare logic.
// This file intentionally left minimal; diff logic lives in internal/diff.
